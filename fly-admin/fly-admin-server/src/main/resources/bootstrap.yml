# 端口配置
server:
  port: 8002
  servlet:
    context-path: /${spring.application.name}
    session:
      timeout: 18000

spring:
  application:
    name: admin
  profiles:
    active: dev

# 暴露端点
management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS